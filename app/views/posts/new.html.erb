<div class="card">
  <h2 class="card-header">Add Post</h2>
  <div class="card-body">
    <%= render "form", post: @post, url: new_post_posts_path(@post), method: :post, back: posts_path  %>
  </div>
</div>
